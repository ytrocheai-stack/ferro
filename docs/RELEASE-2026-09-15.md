# Preparación de producción — 2026-09-15

## Alcance autorizado

Publicar los cambios de `codex/coach-gemini-nvidia`, comprobar ambas cuentas Clerk
permitidas y verificar Gemini preferido y failover a NVIDIA. El propietario cambió
el modelo a **Gemini 3.5 Flash Lite** y eligió el proyecto **YT autoclips**.
Este informe distingue preparación de publicación; no declara finalizado el release.

## Proveedores

- Modelo: `gemini-3.5-flash-lite`, confirmado en la
  [documentación de Google](https://ai.google.dev/gemini-api/docs/models/gemini-3.5-flash-lite).
- Proyecto observado en AI Studio: `gen-lang-client-0124965720` (YT autoclips).
- Nivel observado: gratuito; la interfaz ofrece «Configurar la facturación».
- Cuotas observadas: 15 RPM, 250.000 TPM de entrada, 500 RPD.
- NVIDIA: `deepseek-ai/deepseek-v4-flash-0731`, límite configurado 40 RPM.
- `NVIDIA_API_KEY`, `CLERK_JWT_KEY` y `PSEUDONYMIZATION_KEY` existen en el Worker.
- El propietario guardó `GEMINI_API_KEY` directamente en Wrangler. No se han leído
  ni guardado valores de secretos en chat, repositorio ni archivos.
- Las cuotas son del proyecto Google; otro consumidor del mismo proyecto comparte
  esos límites. El Worker coordina sus propias llamadas y debe respetar los 429.

## Recursos remotos y respaldo

Estado anterior al release:

- Pages: `version.json` devuelve `0cee58c67c1026d8d91970d4d37bc9fba0a830fb`,
  construido el 2026-09-14T06:49:45.565Z.
- Worker activo: `fa2d6eed-654b-48a6-8d07-4fae20332f6f`.
- `/health`: HTTP 200, `ok: true`; esto no valida proveedores ni autenticación.

D1 ya tenía `0014` y `0015`. Se exportó la base a
`.cache/pre-release-2026-09-15.sql` (excluido de Git), con SHA-256
`0f4e8c2325981790d9c90d41a06f24c5b30b71d29622e2b9ba00bc526e6d8c78`.
Bookmark previo: `0000003b-00000000-000050e7-d94b7138c7160bb671c61d7fd484935a`.

Se aplicaron `0016_coach_run_snapshots.sql`, `0017_coach_provider_failover.sql` y
`0018_gemini_reconciliation_state.sql`. La comprobación posterior devuelve cero
migraciones pendientes, las cinco entradas `0014`–`0018` en `d1_migrations` y las
columnas `state_applied`/`state_applied_at` en `gemini_quota_reconciliations`.
No se modificaron IndexedDB, el corpus ni los índices Vectorize.

## Verificación local

- `npm run check` inicial: pasa; 63 suites y 491 pruebas.
- `npm run test:worker`: pasa; 10 suites y 128 pruebas (también incluidas en las 491).
- Dry-run de Wrangler de producción: pasa.
- Dataset fijado regenerado: 1.324 ejercicios, miniaturas y GIFs; snapshots USDA/wger.
- E2E completos: ejecución inicial con seis workers, 30 pasan, 3 omitidos y un
  timeout WebKit al iniciar un entrenamiento libre. El mismo caso pasa tres veces
  aislado y la suite completa con `--workers=2` termina con **31 pasan, 3 omitidos**.
  No se reprodujo de forma aislada; no se atribuye una causa confirmada ni una
  corrección funcional a ese timeout.
- E2E Coach: **1 pasa en Chromium, 1 omitido en WebKit** por la limitación existente
  del Worker simulado. No acredita proveedores ni Clerk reales.
- Se excluyen `test-results` y `playwright-report` de ESLint: las trazas generadas
  incluían JavaScript compilado y hacían fallar `check` durante/después de los E2E.
- Verificación final con Flash Lite: `npm run check` pasa (491 pruebas y build con
  1.386 entradas de precaché); las 128 pruebas Worker y el dry-run final pasan.

## Pendiente para publicar

1. Guardar `GEMINI_API_KEY` directamente en Wrangler; no en chat ni archivos.
2. Terminar la verificación del commit final, integrar en `main` y publicar Pages.
3. Confirmar `version.json` con el SHA publicado y después desplegar Worker con el
   TOML de producción. El cambio de consentimiento exige coordinar ambos releases.
4. Readiness autenticado para las dos cuentas autorizadas; no solicitar sus JWT.
5. Canario mínimo con contexto ficticio, comprobar intentos Gemini en el ledger;
   probar failover controlado a NVIDIA y restaurar el circuito antes de finalizar.
6. Registrar versión Worker, workflow Pages, resultados y límites observados.

## Rollback

- Conservar las migraciones aditivas; no borrar tablas, datos locales ni corpus.
- Si la nueva publicación falla, restaurar la versión Worker anterior indicada
  arriba y un frontend compatible con su consentimiento, mediante el workflow de
  Pages desde `main`. Conservar también el SHA del nuevo release para auditarlo.
- La restauración completa D1 es recuperación de desastre: sobrescribiría escrituras
  posteriores; no forma parte del rollback normal de código.
- Atención: en esta rama `coachReadinessConfiguration` exige ambos proveedores
  habilitados y el orden `gemini,nvidia` en producción. Cambiar solo esos flags
  provoca configuración incompleta; no prometer rollback de un único proveedor
  mediante flags sin revisar ese contrato. Para una prueba temporal de failover,
  usar el circuito durable y restaurar su estado al terminar.
